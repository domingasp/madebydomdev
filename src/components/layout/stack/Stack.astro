---
import { stack, type StackVariants } from "./stack.variants";

type Props = StackVariants & { class?: string };

const props: Props = Astro.props;

const styles = stack(props);
---

<div class={styles}>
  <slot />
</div>
